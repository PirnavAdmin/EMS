// Compatibility stub.
// The single ToastContainer now lives in src/App.jsx so the global toast shell
// is mounted exactly once for the entire application.
export default function ToastProvider() {
  return null;
}
